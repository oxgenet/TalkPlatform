# livekit egress (録音)。音声のみなので軽量。
api_key: REPLACE_WITH_API_KEY
api_secret: REPLACE_WITH_API_SECRET
ws_url: ws://127.0.0.1:7880
redis:
  address: 127.0.0.1:6379
logging:
  level: info
# S3 の接続先は Worker が StartRoomCompositeEgress のリクエストに載せる (RECORDING_S3_*)。
